/**
 * Client-side fallback when settings are not loaded yet.
 * Keep aligned with `REPORTING_CURRENCY_FALLBACK_CODE` in server/lib/org-reporting-money.ts.
 */
export const REPORTING_CURRENCY_FALLBACK_CODE = "ZAR";

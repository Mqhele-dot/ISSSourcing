# Security Evidence Baseline

**Recorded:** 2026-05-19  
**Scope:** Repository state at time of hardening rollout, with current risk status reconciled after the 2026-05-27 update-audit pass.

## Package Manager / Lockfile

| Item | Evidence |
|------|----------|
| Package manager | `npm` via [`package.json`](../../package.json), Dockerfile, and CI |
| Lockfile | [`package-lock.json`](../../package-lock.json); lockfileVersion 3 |

## CI Install / Drift

| Item | Evidence |
|------|----------|
| `npm ci` in CI | [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml), [codespaces-compatibility.yml](../../.github/workflows/codespaces-compatibility.yml), [Dockerfile](../../Dockerfile) |
| Duplicate `npm install` in workflows | Not observed |
| Explicit post-install drift check | `npm ls` sanity after `npm ci` |

## Workflow Permissions / Action Pinning

| Item | Evidence |
|------|----------|
| Narrow permissions | CI uses `permissions: contents: read`; artifact upload grants `actions: write` only where needed |
| Actions pinned | Full commit SHA inventory in [`github-actions-pinning.md`](./github-actions-pinning.md) |

## Supply-Chain Tooling

| Control | Evidence |
|---------|----------|
| Dependency Review / Dependabot | GitHub workflow and Dependabot config tracked in repo |
| npm audit gate | `npm audit --audit-level=high` in supply-chain scripts and CI gate |
| npm signatures / SBOM | `security:audit:signatures` and `security:sbom` scripts |
| Lifecycle script audit | [`scripts/list-lifecycle-scripts.mjs`](../../scripts/list-lifecycle-scripts.mjs) |

## App-Layer Controls

| Item | Evidence |
|------|----------|
| Helmet | [`server/bootstrap/security-middleware.ts`](../../server/bootstrap/security-middleware.ts) |
| CSRF | Session-backed token validation in [`server/services/security-service.ts`](../../server/services/security-service.ts) and [`server/auth.ts`](../../server/auth.ts); deprecated `csurf` removed |
| Rate limiting | [`server/services/security-service.ts`](../../server/services/security-service.ts); path mounts in [`server/routes.ts`](../../server/routes.ts) |
| Upload MIME/size/filename | [`server/http/upload-config.ts`](../../server/http/upload-config.ts) |
| Production error/stack handling | [`server/bootstrap/global-error-handler.ts`](../../server/bootstrap/global-error-handler.ts) |
| Docker non-root runtime | [`Dockerfile`](../../Dockerfile) uses `USER node` and `COPY --chown=node:node` |

## Remaining Prioritized Risks

1. **Lifecycle allowlist hygiene:** expanding native stack must update [`scripts/list-lifecycle-scripts.mjs`](../../scripts/list-lifecycle-scripts.mjs).
2. **Moderate npm audit tail:** high/critical gate is clean; remaining advisories are the dev `esbuild` chain and nested `exceljs`/`uuid`.
3. **CSP style tightening:** production script policy is locked down; style hashing/nonce work remains a future hardening pass. See [`CSP_AND_FRONTEND_NOTES.md`](./CSP_AND_FRONTEND_NOTES.md).
4. **Desktop packaging smoke:** Electron remains supported and should be packaged on the release OS after Electron/native-stack changes.

See the rolling hardening checklist in [`ai-code-security-checklist.md`](./ai-code-security-checklist.md) and current command evidence in [`SECURITY_VERIFICATION_RESULTS.md`](./SECURITY_VERIFICATION_RESULTS.md).

# Shared package

Shared contracts for desktop UI and API.
- `schemas/` contains JSON Schema documents usable by both TS (zod adapters) and Python (pydantic validation).
- `types.ts` contains lightweight frontend types for MVP.
- `models.py` contains matching backend pydantic models.

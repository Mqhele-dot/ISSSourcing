node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
!.env.example

# Local dev: last URL/port written by server on listen (see server/index.ts)
.local-dev-url
.codespace-app-url

# Playwright (local reports; do not commit)
playwright-report/
test-results/

# Locally generated CycloneDX SBOM (`npm run security:sbom`); CI uploads artifact instead.
sbom.cdx.json

# Local npm audit snapshots (evidence regenerated ad hoc; summarize in docs/security/audit-remediation-backlog.md)
audit-high*.json
audit-high*.txt
.npm-cache-audit/
.npm-cache-local/
.npm-cache/
.electron-cache-local/
.electron-builder-cache-local/

# Local runtime artifacts from diagnostics, smoke tests, and generated exports.
.control-tower-*.log
output/
tmp/
uploads/exports/
